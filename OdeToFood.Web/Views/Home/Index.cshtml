@model IEnumerable<OdeToFood.Data.Models.Restaurant>
@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>ASP.NET</h1>
    <div>Hello @Model.Count()</div>
</div>

<ul>
    @foreach (var r in Model)
    {
        <li>@r.Name</li>
    }
</ul>